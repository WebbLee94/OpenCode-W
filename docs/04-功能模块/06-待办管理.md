# 待办管理

> 新增于 v1.4

## 设计目标

查看 AI 助手的待办清单，从待办视角理解会话目标与执行进度。

## 实际布局

### 独立页面（侧边栏 `Todos`）

```
┌─ Todos ──────────────────────────────────────────────────────┐
│  [🔍 搜索待办内容...]  [状态: 全部 ▾]  [优先级: 全部 ▾]     │
│  共 915 条待办                                               │
├──────────────────────────────────────────────────────────────┤
│  # │ 待办内容                     │ 状态     │ 优先级 │ 会话  │
│  1 │ 修复 login 页面样式          │ ✅ 完成  │ 🔴高   │ ses_… │
│  2 │ 添加用户注册功能              │ 🔄 进行中│ 🟡中   │ ses_… │
│  3 │ 编写单元测试                  │ ⏳ 待处理│ 🟢低   │ ses_… │
│  …                                                           │
├──────────────────────────────────────────────────────────────┤
│           < 上一页  1/19  下一页 >   每页 50 条               │
└──────────────────────────────────────────────────────────────┘
```

### Session 详情面板内嵌（Tab 形式）

```
┌─ 会话详情 ───── [基本] [Token] [Tool] [Skill] [📋Todos(5)] ─┐
│  ┌─ [全部] [待处理] [进行中] [已完成] ─── Tab 筛选 ──────┐  │
│  │  1. [🟡中] [🔄] 修复 login 页面样式                   │  │
│  │  2. [🔴高] [✅] 添加用户注册功能                       │  │
│  │  3. [🟢低] [⏳] 编写单元测试                           │  │
│  └───────────────────────────────────────────────────────┘  │
└──────────────────────────────────────────────────────────────┘
```

## 数据来源

| 数据 | IPC 通道 | SQL / 来源 |
|------|---------|-----------|
| 全局待办列表 | `todos:list` | `SELECT t.*, s.title FROM todo t JOIN session s ... WHERE content LIKE ? AND status = ? AND priority = ? ORDER BY time_created DESC` |
| 单会话待办 | `todos:bySession` | `WHERE t.session_id = ? ORDER BY position ASC` |

## 表格字段

| 列 | 来源 | 说明 |
|----|------|------|
| 待办内容 | `todo.content` | 截断 120 字符 |
| 状态 | `todo.status` | 彩色 badge：⏳待处理/🔄进行中/✅完成/🚫取消 |
| 优先级 | `todo.priority` | 彩色 badge：🔴高/🟡中/🟢低 |
| 所属会话 | JOIN `session.title` | 点击跳转 `/sessions/{sessionId}` |

## 筛选能力

| 筛选器 | 实现 | 说明 |
|--------|------|------|
| 搜索 | `WHERE content LIKE ?` | 实时防抖 300ms |
| 按状态 | `WHERE status = ?` | 下拉：全部/待处理/进行中/已完成/已取消 |
| 按优先级 | `WHERE priority = ?` | 下拉：全部/高/中/低 |
| 分页 | `LIMIT ? OFFSET ?` | 默认 50 条/页 |

## 交互

- **点击行**：跳转到该会话的消息页面（`/sessions/{sessionId}/messages`）
- **状态/优先级**：只读展示，不支持编辑（v1.4 第一版）
