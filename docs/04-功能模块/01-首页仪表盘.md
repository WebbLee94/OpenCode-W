# 首页仪表盘

## 设计目标

打开应用 3 秒内看清数据库全貌。

## 实际布局

```
┌──────────────────────────────────────────────────────┐
│  Dashboard                              [VACUUM] [检查点] [刷新] │
│                                                      │
│  ┌────────┐ ┌────────┐ ┌────────┐ ┌───────┐         │
│  │844 MB  │ │2,858   │ │20      │ │77%    │         │
│  │总大小  │ │会话数  │ │项目数  │ │可回收 │         │
│  └────────┘ └────────┘ └────────┘ └───────┘         │
│  ┌────────┐ ┌────────┐ ┌────────┐ ┌───────┐         │
│  │12 MB   │ │136K    │ │$42.50  │ │85%    │         │
│  │WAL日志 │ │Part行  │ │估算成本│ │缓存命中│         │
│  └────────┘ └────────┘ └────────┘ └───────┘         │
│                                                      │
│  ┌── Token 分布 ─────────┐ ┌── 工具使用排行 TOP 10 ─┐│
│  │  [饼图] 输入/输出/推理  │ │  [横向柱状图]         ││
│  │  输入Token  1.2B       │ │  bash      12,450     ││
│  │  输出Token  320M       │ │  read       7,832     ││
│  │  推理Token  45M        │ │  write      5,201     ││
│  │  缓存命中率 85%        │ │  ...                   ││
│  └────────────────────────┘ └────────────────────────┘│
│                                                      │
│  ┌── 技能使用分布 ────────┐ ┌── 增长趋势 (近30天) ──┐│
│  │  [横向柱状图]          │ │  [双Y轴折线图]         ││
│  │  brainstorming   89   │ │  新会话数 + 数据增长    ││
│  │  tdd             56   │ │                        ││
│  │  ...                   │ │                        ││
│  └────────────────────────┘ └────────────────────────┘│
└──────────────────────────────────────────────────────┘
```

## 数据来源

| 指标 | IPC 通道 | SQL / 来源 |
|------|---------|-----------|
| 总大小 | `dashboard:overview` | `fs.statSync(path).size` |
| 会话数 | `dashboard:overview` | `SELECT COUNT(*) FROM session` |
| 项目数 | `dashboard:overview` | `SELECT COUNT(DISTINCT project_id) FROM session WHERE project_id IS NOT NULL` |
| 可回收碎片 | `dashboard:overview` | `PRAGMA freelist_count * PRAGMA page_size` |
| WAL 大小 | `dashboard:overview` | `fs.statSync(path + '-wal').size` |
| Part 行数 | `dashboard:overview` | `SELECT COUNT(*) FROM part` |
| Token 总计 | `dashboard:tokens` | `SELECT SUM(tokens_input), SUM(tokens_output), SUM(tokens_reasoning) FROM session` |
| 估算成本 | `dashboard:tokens` | `SELECT SUM(cost) FROM session` |
| 缓存命中率 | `dashboard:tokens` | `SUM(tokens_cache_read) / (SUM(tokens_input) + SUM(tokens_cache_read)) * 100` |
| 工具排行 | `dashboard:toolRanking` | `GROUP BY json_extract(data, '$.tool_name')` WHERE type='tool' |
| Skill 分布 | `dashboard:skillUsage` | `WHERE json_extract(data, '$.tool_name')='skill'` |
| 增长趋势 | `dashboard:trends` | 按天 `GROUP BY date(time_created/1000, 'unixepoch')` |

## 图表实现

| 图表 | 库 | 类型 | 说明 |
|------|---|------|------|
| Token 分布 | Recharts | PieChart (环形) | 输入/输出/推理三色饼图 + 右侧指标列表 |
| 工具排行 | Recharts | BarChart (横向) | 蓝色柱状图，TOP 10 |
| 技能分布 | Recharts | BarChart (横向) | 紫色系多色柱状图，TOP 10 |
| 增长趋势 | Recharts | LineChart (双Y轴) | 左轴：新会话数，右轴：累计数据增长 |

## 交互

- **一键 VACUUM**：调用 `database:vacuum`，完成后弹窗显示释放空间
- **WAL Checkpoint**：调用 `database:checkpoint`，将 WAL 日志合并回主数据库
- **刷新**：重新并行加载所有仪表盘数据
- 未连接数据库时显示引导页面，提供"Connect Database"按钮

## 刷新策略

- 页面加载时并行拉取所有数据（`Promise.all`）
- 手动刷新按钮
- 不自动轮询（避免锁定数据库）
- 操作（VACUUM/Checkpoint）完成后自动刷新

## 数据加载流程

```
1. 检查数据库连接 (database:health)
2. 若已连接，并行请求:
   - dashboard:overview → DatabaseStats
   - dashboard:tokens → TokenStats
   - dashboard:toolRanking → ToolRanking[]
   - dashboard:skillUsage → SkillUsage[]
   - dashboard:trends → TrendDataPoint[]
3. 渲染统计卡片和图表
4. 若任一请求失败，显示错误页面 + 重试按钮
```
